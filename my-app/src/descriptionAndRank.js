class DescriptionAndRank {
  constructor(description, rank) {
    this.description = description;
    this.rank = rank;
  }

  setDescription(description) {
    this.description = description;
  }

  setRank(rank) {
    this.rank = rank;
  }
}

export default DescriptionAndRank;
